import pandas as pd

from typing import Tuple, Union, List

class DelayModel:

    def __init__(
        self
    ):
        self._model = None # Model should be saved in this attribute.

    def preprocess(
        self,
        data: pd.DataFrame,
        target_column: str = None
    ) -> Union(Tuple[pd.DataFrame, pd.DataFrame], pd.DataFrame):
        """
        Preparar datos sin procesar para entrenamiento o predicción.

        Args:
        data (pd.DataFrame): datos sin procesar.
        target_column (str, opcional): si se configura, se devuelve el objetivo.

        Devuelve:
        Tuple[pd.DataFrame, pd.DataFrame]: características y objetivo.
        o
        pd.DataFrame: características.
        """
        return

    def fit(
        self,
        features: pd.DataFrame,
        target: pd.DataFrame
    ) -> None:
        """
        Ajuste del modelo con datos preprocesados.

        Argumentos:
        features (pd.DataFrame): datos preprocesados.
        target (pd.DataFrame): target.
        """
        return

    def predict(
        self,
        features: pd.DataFrame
    ) -> List[int]:
        """
        Predecir demoras para nuevos vuelos.

        Args:
        features (pd.DataFrame): datos preprocesados.

        Retornos:
        (List[int]): objetivos previstos.
        """
        return